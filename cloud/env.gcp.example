# GCP Production Environment Variables
# Copy these values to your GCP Cloud Run service

# Database Configuration
DATABASE_URL=sqlite:///app/data/db.sqlite
CLOUD_STORAGE_BUCKET=finreview-app-storage

# Google OAuth (from Secret Manager)
GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}

# JWT Configuration (from Secret Manager)
JWT_SECRET_KEY=${JWT_SECRET_KEY}

# Frontend URL (will be your Firebase Hosting URL)
FRONTEND_URL=https://your-project-id.web.app

# CORS Origins
CORS_ORIGINS=https://your-project-id.web.app,https://your-project-id.firebaseapp.com

# Environment
ENVIRONMENT=production

# GCP Project Configuration
GCP_PROJECT_ID=finreview-app
GCP_REGION=us-central1

# Cloud Run Configuration
PORT=8080
HOST=0.0.0.0
